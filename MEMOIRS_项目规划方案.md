# MEMOIRS / 回忆录平台 · 项目规划方案

---

## A. 项目总体判断

### 对这个项目本质的判断

MEMOIRS 的本质是一个**人生素材管理引擎 + AI 叙事重建系统**，而不是相册、博客或聊天工具。它要解决的核心问题是：普通人有大量零散的人生素材（照片、录音、记忆片段），但缺乏能力将它们组织成有叙事结构的作品。AI 在这里的角色是**叙事建筑师**，不是装饰品。

已有的 UI 质量相当高——视觉语言统一、交互层级清晰、中文排版考究。它已经定义了产品的调性和页面骨架，是宝贵的资产，不可浪费。但当前 UI 存在一个根本性问题：**所有数据都是静态 mock，没有真实的数据流动**。整个系统的核心链路——素材上传 → 解析 → 事件提取 → 章节生成——目前一环都没有真正跑通。

### 当前最关键的问题

1. **没有后端**：所有 AI 调用直接在前端用 localStorage 存 API Key，这无法支撑真实用户。
2. **数据模型未定义**：素材、事件、章节、项目之间的关系没有建模，未来任何功能扩展都会遇到结构混乱。
3. **核心链路未闭环**：用户上传一段语音，最终怎么变成回忆录章节的一个段落？这条路目前是断的。

### 最正确的推进策略

**"先打通主链路，再补全体验"**。V1 的目标只有一个：让一个用户能完成"上传素材 → 看到 AI 生成的章节草稿"这个最小闭环。不要在这之前做任何多端适配、精美导出、复杂权限系统。

---

## B. 项目规划方案

### 项目定义

> MEMOIRS 是一个面向普通人的多模态回忆录创作平台。用户通过上传视频、语音、照片和文字，由 AI 协助完成素材解析、事件提炼、时间线组织和章节撰写，最终生成一份可阅读、可分享、可打印的个人传记。

### 用户价值

| 用户痛点 | MEMOIRS 的解法 |
|---|---|
| 有素材，不知怎么整理 | 自动解析素材，提取关键事件 |
| 想写回忆录，不会写 | AI 根据素材生成章节草稿 |
| 素材散落各处，难以归档 | 统一的项目-章节-素材三层管理 |
| 写了一半，坚持不下去 | AI 主动提示缺口，给出写作引导 |

### MVP 范围（V1 必须有，其余延后）

**必须做：**
- 用户注册/登录（手机号或邮箱，JWT）
- 创建回忆录项目
- 上传素材（图片、音频、文本）
- AI 解析素材，提取关键信息（时间、地点、人物、事件摘要）
- 基于素材生成章节草稿
- 章节手动编辑
- 基础预览与导出（PDF 或 HTML）

**延后：**
- 视频上传与转录（需要大量存储和计算，成本高，放 V2）
- 精美印刷导出
- 分享给家人
- 微信小程序
- 多语言

### 核心闭环

```
用户上传素材
    ↓
后端存储素材文件（OSS）+ 元数据（DB）
    ↓
AI 队列任务：解析素材（识别时间/地点/人物/情绪）
    ↓
生成"事件卡片"（Event Card）
    ↓
用户确认/编辑事件卡片
    ↓
AI 将相关事件卡片聚合，生成章节草稿
    ↓
用户在写作界面编辑章节
    ↓
生成完整回忆录，导出
```

### 核心领域对象设计

```
User（用户）
 └── Project（回忆录项目）  ← 每个用户可有多个项目
      ├── Asset（素材）     ← 照片/音频/文字/视频
      │    └── AssetAnalysis（AI 解析结果）
      ├── Event（事件卡片） ← 从素材中提取的关键事件
      └── Chapter（章节）  ← 关联多个 Event，包含 AI 草稿和人工编辑内容
```

**关键关系：**
- 一个 Asset 可以关联多个 Event
- 一个 Event 可以出现在多个 Chapter（避免信息孤岛）
- Chapter 有"草稿"和"已发布"两个状态
- Project 有时间线视图（基于 Event 的时间戳排列）

---

## C. 技术与架构建议

### 前端

**保留现有 React + Vite + Tailwind 技术栈**，这套组合成熟、轻量、适合快速迭代。

需要做的改造：
1. **移除所有 mock 数据**，替换为真实 API 调用（用 React Query 或 SWR 管理服务端状态）
2. **建立 API 层**：新建 `src/api/` 目录，将所有后端调用封装为独立函数，禁止在组件里直接 fetch
3. **状态管理**：当前 localStorage 存 API Key 的做法只能用于开发。生产环境应改为 JWT Token 存储，由后端鉴权
4. **路由守卫**：添加未登录跳转 `/login` 的逻辑
5. **文件上传组件**：当前 Capture 页面是 mock，需要接入真实 multipart 上传

**不建议现在做：**
- 不要用 Next.js 重写（成本高，收益低，当前阶段不需要 SSR）
- 不要接入 Redux（React Query 已经够用）

### 后端

**推荐：Node.js + TypeScript + Express（或 Fastify）**

原因：
- 与前端同语言，AI 员工切换成本低
- TypeScript 保证接口类型一致
- 生态成熟，阿里云 ECS 部署简单

核心服务模块：

```
auth-service      → 用户注册/登录/JWT
project-service   → 项目 CRUD
asset-service     → 素材上传/管理
analysis-service  → AI 解析队列（异步）
event-service     → 事件卡片 CRUD
chapter-service   → 章节生成与编辑
export-service    → 导出 PDF/HTML
```

AI 调用策略：
- **不在前端直接调用 AI**（安全问题，成本不可控）
- 所有 AI 调用统一走后端，后端持有 API Key
- 解析任务放入队列（Redis + BullMQ），异步处理，前端轮询状态

### 数据库

**主库：PostgreSQL**
- 结构化数据（用户、项目、事件、章节）
- 支持 JSON 字段（存储 AI 解析的非结构化结果）
- 阿里云 RDS PostgreSQL，开箱即用

**缓存/队列：Redis**
- AI 任务队列
- Session 缓存
- 阿里云 Redis，按需开通

### 文件存储

**阿里云 OSS**
- 所有素材文件（图片、音频）存 OSS
- 后端生成签名 URL，前端直传 OSS（减少后端带宽压力）
- 导出的 PDF 也存 OSS，生成临时下载链接

### 云部署

**阿里云部署架构（V1 最小化）：**

```
用户浏览器
    ↓
阿里云 SLB（负载均衡）
    ↓
ECS（2核4G 起步）
  ├── Nginx（静态前端 + 反向代理）
  ├── Node.js 后端服务（PM2 守护）
  └── Redis（可用阿里云托管版）
    ↓
RDS PostgreSQL（阿里云托管）
OSS（素材文件存储）
```

域名 + HTTPS 用阿里云 SSL 证书，免费版够用。

### 多端预留

- 后端从第一天起就做成**纯 REST API**（或 GraphQL），禁止任何服务端渲染逻辑混入
- 前端禁止把业务逻辑写在页面组件里，统一提取到 `src/services/` 和 `src/api/`
- 这样未来小程序只需要接同一套 API，无需改后端

---

## D. 版本路线图

### V0：结构化整理现有 UI（当前任务，1-2 周）

目标：让现有前端代码具备"可接后端"的条件

- [ ] 清理所有 mock 数据，替换为 API 占位函数
- [ ] 建立 `src/api/` 层，定义接口契约（即使后端还没好）
- [ ] 建立 `src/types/` 层，定义 TypeScript 类型（User, Project, Asset, Event, Chapter）
- [ ] 添加路由守卫（未登录跳转）
- [ ] 梳理现有页面与最终产品页面的对应关系，标注哪些页面 V1 需要，哪些延后
- [ ] 确定 API 接口文档草案（用 OpenAPI 格式或简单 Markdown 均可）

**V0 验收标准：** 前端代码中不存在硬编码的 mock 数据；所有数据获取都通过统一的 API 函数调用；TypeScript 类型无报错。

---

### V1：Web MVP 闭环（6-8 周）

目标：真实用户可以注册、上传素材、看到 AI 生成的章节草稿

**后端任务：**
- [ ] 用户注册/登录（JWT）
- [ ] 项目 CRUD
- [ ] 素材上传（OSS 直传签名 + 元数据入库）
- [ ] 图片解析：调用视觉 AI 提取描述、时间、人物
- [ ] 音频解析：语音转文字（阿里云 ASR 或 Gemini）
- [ ] 文本素材直接入库
- [ ] 事件卡片生成（基于解析结果）
- [ ] 章节草稿生成（基于事件卡片聚合）
- [ ] 章节编辑保存
- [ ] 基础导出（HTML 格式）

**前端任务：**
- [ ] 接通登录注册流程
- [ ] 接通项目创建/列表
- [ ] 接通素材上传（Capture 页面）
- [ ] 素材解析状态展示（进度轮询）
- [ ] 事件卡片展示与编辑
- [ ] 章节编辑器接通后端（Writing 页面）
- [ ] 时间线页面接通真实数据（Timeline 页面）
- [ ] 基础导出按钮

**V1 验收标准：** 一个外部用户（非开发者）能完成"注册 → 创建项目 → 上传3张照片 → 看到AI生成的章节草稿 → 手动编辑 → 导出"的完整流程，全程无需开发者介入。

---

### V2：体验增强 + 移动端优化（4-6 周）

- 视频上传与转录
- 语音录制直传（前端 MediaRecorder API）
- 移动端响应式优化（现有 UI 已有 MobileNav，需完善）
- AI 润色功能完善（Refinement 页面接通）
- 分享预览（生成公开链接）
- 用户数据统计（Analytics 页面接通）
- 精美 PDF 导出

---

### V3：微信小程序（8-12 周）

- 评估是否用 Taro 或 UniApp 复用逻辑层（推荐 Taro，与 React 技术栈一致）
- 小程序核心功能：拍照上传、语音录制、章节浏览
- 写作功能降级（小程序键盘体验差，以查看为主）
- 微信登录对接
- **注意：** 后端 API 无需改动，这是 V0 设计的收益

---

### V4：App 预留方向

- 评估 React Native（与现有 React 代码共享逻辑层）
- 或评估 Flutter（性能更好，但需要重写 UI）
- 离线功能（本地草稿，网络恢复后同步）
- 推送通知（AI 处理完成提醒）
- 此阶段需要根据 V1-V3 的用户反馈重新定义需求，现在无需详细规划

---

## E. 第一轮执行建议

### 应先做什么（V0 阶段，按顺序）

**第 1 步（项目 Owner）：**
- 定义核心数据类型（TypeScript interface）：User、Project、Asset、Event、Chapter
- 输出 API 接口文档草案（每个接口：路径、方法、入参、出参）
- 这份文档是后续所有 AI 员工的"合同"

**第 2 步（前端 AI）：**
- 基于接口文档，在 `src/api/` 建立接口函数（用 axios 或 fetch 封装）
- 在 `src/types/` 建立类型文件
- 替换各页面中的 mock 数据为 API 函数调用（先 loading/error 状态占位）
- 添加路由守卫

**第 3 步（后端 AI）：**
- 搭建 Express + TypeScript 项目骨架
- 实现用户注册/登录
- 实现项目 CRUD
- 实现素材上传（OSS 签名接口）
- 数据库建表（基于第 1 步定义的类型）

**第 4 步（前后端联调）：**
- 登录流程打通
- 素材上传打通
- 这两个点通了，整个系统的骨架就活了

### 不该先做什么

- 不要现在做微信登录
- 不要现在做 PDF 导出（先用 HTML）
- 不要现在做视频处理（存储和计算成本高）
- 不要现在优化 SEO 或做 SSR
- 不要现在设计复杂的权限系统（V1 只有 owner 角色）
- **不要让 AI 员工在没有接口文档的情况下开始写代码**

### 第一轮验收标准

| 验收项 | 标准 |
|---|---|
| 数据类型定义 | TypeScript 类型文件已建立，覆盖所有核心实体 |
| 接口文档 | 至少覆盖：登录、项目、素材上传、章节的 CRUD |
| 前端 API 层 | `src/api/` 目录已建立，所有接口有对应函数 |
| Mock 清理 | 页面组件中无硬编码数据，统一走 API 函数 |
| 路由守卫 | 未登录访问受保护页面自动跳转 `/login` |

---

## F. 风险与决策点

### 当前最大风险

**风险 1：AI 解析成本失控**
语音转文字、图片识别、章节生成，每一步都消耗 Token。如果没有限流和成本控制，V1 上线后可能迅速超出预算。
→ 对策：每个用户设置月度配额；AI 调用全部走后端，可监控和限制；优先选用性价比高的模型（Gemini Flash 而非 Pro）。

**风险 2：数据模型定义过晚**
如果前后端各自开发，最后发现对"事件"的理解不一致，联调时会很痛苦。
→ 对策：**数据类型定义必须是 V0 阶段的第一个产出**，任何人开始写代码之前，类型文件必须已经确定。

**风险 3：现有 UI 与产品逻辑脱节**
当前 UI 有些页面（如 BiographyBook、Preview）设计精美但功能定义模糊。如果让 AI 员工直接接后端，容易走偏。
→ 对策：V0 阶段做一次"页面功能映射"，明确每个页面在 V1 是"实现"还是"延后"。

### 容易走偏的地方

1. **过早追求 UI 完美**：现有 UI 已经很好了，不要在数据没通之前继续美化页面
2. **AI 员工擅自扩展功能**：必须给每个任务设定明确边界，否则 AI 会自作主张加功能
3. **后端一开始就做微服务**：V1 用单体服务，不要过度设计
4. **把"AI 润色"当核心功能**：润色是加分项，数据链路才是核心，不要本末倒置

### 必须尽快决定的事项

| 决策项 | 建议 | 截止时间 |
|---|---|---|
| AI 服务提供商选择 | 建议 Gemini（已有集成）+ 阿里云 ASR（语音转写） | V0 结束前 |
| 云服务商确认 | 已定阿里云，需要开通 ECS + RDS + OSS + Redis | V1 开始前 |
| 域名与备案 | 中国大陆部署必须备案，周期 20 天左右 | **立即启动** |
| 用户数据归属与隐私政策 | 涉及用户真实人生数据，需要隐私条款 | V1 上线前 |
| MVP 目标用户确认 | 建议先定向邀请 10-20 个家庭用户内测 | V1 上线前 |

---

> **本文档用途：** 可直接作为后续交给前端 AI、后端 AI 的上游依据。建议将「E. 第一轮执行建议」和「B. 核心领域对象设计」部分单独提取，作为下一个 AI 员工的 System Prompt 前置上下文。
>
> **文档版本：** V0.1 · 初稿
> **生成日期：** 2026-03-21
