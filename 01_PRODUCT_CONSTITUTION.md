# 01_PRODUCT_CONSTITUTION

- 状态：Owner approved for downstream planning
- 版本：v0.1
- 日期：2026-03-21
- 文档角色：本文件是 MEMOIRS 的产品宪章与上游约束。后续 PRD、技术方案、任务拆分、agent prompt 如与本文件冲突，以本文件为准，直到被明确修订。

## 0. 裁决结论

### 0.1 两份文档各自的角色

- `[Untitled-1.md]` 是方法论文档，不是产品结论。它定义了工作方式、输出格式、讨论顺序、AI 协作约束。
- `[MEMOIRS_项目规划方案.md]` 是按该方法产出的一版产品与技术方案初稿。

### 0.2 本次裁决

- 采纳 `[Untitled-1.md]` 作为后续所有 agent 的规划方法与输出标准。
- 采纳 `[MEMOIRS_项目规划方案.md]` 作为产品方向基线。
- 但必须做 5 个修正，否则方案会与真实推进路径脱节：

1. 产品推进轴要拆成两条：
   - 用户推广轴：先自己用，再给家里人用，最后面向公众上线。
   - 平台交付轴：先 Web，再移动 Web，再小程序，再 App 预留。
2. V1 的目标用户不是“普通公众”，而是“你自己这个 owner”。这会直接影响认证、权限、导出、错误容忍度与排期。
3. 家庭阶段不是“公开分享”的弱版本，而是“受邀请的协作与查看”阶段，权限模型必须单独考虑。
4. 现有 UI 里 `BiographyBook`、`Preview`、`Refinement`、`Analytics`、`Notifications` 的优先级被高估了。V1 应优先打通“素材 -> 事件 -> 章节”，不是先做展示型页面。
5. `Subject` 概念必须进入宪章。账号使用者不一定等于回忆录主人公。未来你可能替父母写，也可能让家人一起补充你的回忆录。如果没有这个概念，后续家庭阶段会返工。

### 0.3 Owner 已确认的裁决

- 登录方式：已选择 `方案 B`
  - 采用邮箱登录或 magic link
- 家庭阶段协作模型：已选择 `方案 A`
  - 一个项目，多人受邀协作
- AI 供应栈方向：已确认
  - 不锁定单一供应商
  - 系统必须提供多模型接入能力
  - 后续可接入 Gemini、Claude、OpenAI、DeepSeek、Kimi、MiniMax 等供应商
- V1 多模型最小落地方式：已选择 `方案 A`
  - 先做统一接口
  - V1 只接 2 个 provider 作为起点
  - 优先验证 provider 抽象与主链路闭环，不追求一次接全

## 1. 第一性原则判断

### 1.1 产品本质

MEMOIRS 不是相册，不是博客，不是聊天产品，也不是单纯的 AI 润色器。

它的本质是：

> 一个把分散的人生素材，转化为可编辑、可组织、可输出的回忆录作品的多模态叙事生产系统。

### 1.2 真正要解决的问题

用户真正缺的不是“存储空间”，而是下面 4 件事：

1. 不知道如何把零散素材整理成可用材料。
2. 不知道如何从材料中提炼出“事件”。
3. 不知道如何把事件组织成时间线和章节。
4. 不知道如何把章节继续写下去并最终成册。

所以核心价值不是“上传成功”，而是：

> 让用户从零散素材，走到一份可读、可改、可继续完善的回忆录草稿。

### 1.3 AI 的角色

- AI 不是主作者。
- AI 是素材分析员、事件整理员、章节起草员、写作助手。
- 最终作品的编辑权必须始终在用户手里。

## 2. 产品定义

### 2.1 一句话定义

MEMOIRS 是一个帮助用户把照片、语音、文字等人生素材整理为事件、时间线与章节，并逐步形成完整回忆录的多模态创作平台。

### 2.2 核心承诺

用户不需要先会写作，也不需要先会整理档案，只需要不断上传和校正素材，系统就能逐步生成一份可以继续编辑的回忆录。

### 2.3 绝不偷换的目标

后续任何 agent 都不能把项目偷换成：

- 相册管理器
- 私人笔记本
- AI 聊天界面
- 单纯排版工具
- 单纯家谱或家族社交产品

## 3. 推进总路线

### 3.1 用户推广轴

### Stage A：Owner Private Alpha

- 目标用户：你自己
- 目标：证明核心闭环对真实人生素材可用
- 必须达成：
  - 你能创建至少 1 个回忆录项目
  - 你能上传图片、音频、文字
  - 系统能生成事件与章节草稿
  - 你能手动修订并导出初稿
- 此阶段不追求：
  - 家庭协作
  - 公共分享
  - 完美移动端
  - 精美印刷导出

### Stage B：Family Invite Beta

- 目标用户：你邀请的家庭成员
- 目标：验证多人参与回忆录补充与查看是否成立
- 必须达成：
  - 家人可以被邀请进入指定项目
  - 家人可按角色上传素材、补充回忆、查看章节或评论
  - 权限边界明确，不误删、不串项目
- 此阶段不追求：
  - 完整开放注册
  - 公共搜索与广场

### Stage C：Public Launch

- 目标用户：非熟人公众用户
- 目标：从“你家可用”升级为“陌生用户也能独立完成核心链路”
- 必须达成：
  - 自助注册/登录
  - 基础成本控制
  - 基础风控与配额
  - 隐私条款、服务条款、数据保留策略

### 3.2 平台交付轴

- V0：现有 UI 结构化整理，建立真实产品壳
- V1：Web owner 闭环版
- V2：Family beta + 移动 Web 强化
- V3：微信小程序
- V4：App 预留

说明：

- 用户推广轴和平台交付轴不是一回事。
- 当前应该做的是：`V0 -> V1`，服务 `Stage A`。

## 4. V1 的正式定义

### 4.1 V1 成功的唯一标准

V1 不是“页面很多”，而是：

> 你本人能在不需要开发者介入的前提下，完成“创建项目 -> 上传真实素材 -> 获得事件与章节草稿 -> 手动修订 -> 导出初稿”的完整流程。

### 4.2 V1 必做能力

- 真实账户体系，至少支持 owner 使用
- 创建项目
- 定义回忆录主人公基本信息
- 上传素材：图片、音频、文字
- 素材入库与状态可追踪
- AI 解析素材
- 从素材生成事件卡片
- 事件卡片人工校正
- 从事件生成章节草稿
- 章节编辑与保存
- 基础预览
- 基础导出，优先 HTML，其次简单 PDF

### 4.3 V1 明确延后

- 视频上传与视频理解
- 公开分享页
- 家庭协作权限
- 精美印刷排版
- 独立 Analytics 页面
- 独立 Notifications 页面
- 独立 QuickActions 页面
- 小程序
- App

### 4.4 V1 的非目标

- 不是用来证明 AI 很炫
- 不是用来证明页面很漂亮
- 不是用来证明可以接很多模型
- 不是用来证明可以导出精装书

## 5. 核心产品原则

1. 先打通链路，再做体验锦上添花。
2. 先数据建模，再写页面细节。
3. 先 owner 单人可用，再谈家庭协作。
4. 先 Web 真流程，再谈多端复用。
5. AI 输出默认都是草稿，必须可编辑、可撤销、可重生成。
6. 原始素材必须保留，AI 解析结果只是派生层。
7. 时间线是组织视图，不是必须先完成的阻塞步骤。
8. 回忆录的最小叙事单位是 `Event`，不是 `Asset`，也不是“聊天消息”。

## 6. 核心用户与角色

### 6.1 角色定义

- Owner：项目所有者，拥有全部权限。V1 唯一必须实现的角色。
- Contributor：被邀请补充素材或回忆的人。V2 起考虑。
- Viewer：只读查看者。V2 起考虑。

### 6.2 主人公与使用者必须区分

- `AccountUser`：谁在登录、操作系统。
- `Subject`：这份回忆录写的是谁。

强制规则：

- 一份 `Project` 对应一个主 `Subject`。
- `Subject` 在 V1 可以先内嵌在 `Project` 中实现，不要求一开始独立成复杂实体。
- 但在概念上必须明确区分，否则家庭阶段会出现“子女代父母整理回忆录”的结构问题。

## 7. 核心领域对象

### 7.1 必须存在的对象

- `AccountUser`
- `Project`
- `SubjectProfile`
- `Asset`
- `AssetAnalysis`
- `Event`
- `Chapter`
- `ExportVersion`
- `Job`

### 7.2 对象定义

#### `AccountUser`

- 登录身份
- 拥有项目
- 未来可被授予不同项目角色

#### `Project`

- 一份回忆录工程
- 是所有素材、事件、章节、导出的顶层容器
- 包含主人公基础信息、状态、封面信息、阶段信息

#### `SubjectProfile`

- 回忆录主人公信息
- 至少包含：姓名、称呼、出生年份或年代区间、与 owner 关系、叙事口吻偏好

#### `Asset`

- 原始素材
- 类型：image、audio、text，video 延后
- 必须有上传状态与处理状态

#### `AssetAnalysis`

- AI 对某份素材的结构化解析结果
- 例如：时间、地点、人物、摘要、情绪、标签、置信度
- 允许多次重跑与版本化

#### `Event`

- 叙事原子
- 由一个或多个素材推断而来
- 必须可人工编辑
- 是章节生成的唯一直接输入

#### `Chapter`

- 叙事单元
- 由一个或多个事件组成
- 包含：标题、摘要、草稿内容、人工编辑内容、排序、状态

#### `ExportVersion`

- 可预览或导出的版本快照
- 避免把“当前编辑态”与“导出结果”混为一谈

#### `Job`

- 异步任务
- 用于上传后解析、事件生成、章节生成、导出

### 7.3 关键关系

- 一个 `Project` 有多个 `Asset`
- 一个 `Asset` 可关联多个 `Event`
- 一个 `Event` 可引用多个 `Asset`
- 一个 `Chapter` 可聚合多个 `Event`
- 一个 `Event` 可以暂时未归属章节
- `Chapter` 的 AI 草稿与人工编辑内容要分层保存

### 7.4 不可破坏的约束

1. 任何章节都不能只依赖“即时 prompt”存在，必须能回溯到事件层。
2. 任何事件都不能只以自然语言长文存在，至少要有结构化骨架。
3. 原始素材删除必须谨慎，因为会影响分析、事件、章节可追溯性。
4. AI 生成失败时，用户仍应能手动创建事件和章节。

## 8. 核心闭环

### 8.1 标准闭环

1. 创建项目
2. 设置主人公基本信息
3. 上传素材
4. 系统存储原始素材与元数据
5. 系统异步生成素材分析
6. 系统从分析结果生成可编辑事件
7. 用户确认、补充、修正事件
8. 系统按事件聚合生成章节草稿
9. 用户编辑章节
10. 生成预览与导出版本

### 8.2 对链路的裁决

- `Timeline` 是步骤 7 和步骤 8 之间的重要组织视图，但不是必须的阻塞关卡。
- `Refinement` 不应作为独立主链路页面优先实现，它应先作为 `Writing` 内的辅助能力存在。
- `BiographyBook` 不应早于闭环完成。它属于“导出体验层”，不是“核心生产链”。

## 9. 页面与模块裁决

### 9.1 V1 保留并接真数据

- `Home`
  - 角色：项目总览与继续创作入口
- `Login`
  - 角色：真实登录入口
- `Onboarding`
  - 角色：项目或主人公初始化，不再负责本地 API Key 配置
- `Archive`
  - 角色：素材库与整理中心
- `Capture`
  - 角色：多模态素材采集入口
- `Timeline`
  - 角色：按事件组织的时间视图
- `Writing`
  - 角色：章节编辑主界面
- `Preview`
  - 角色：基础预览与导出入口
- `Settings`
  - 角色：账户、项目、隐私设置

### 9.2 V1 合并或降级

- `Refinement`
  - 裁决：不作为独立 V1 主页面
  - 处理方式：先并入 `Writing` 右侧助手面板或章节工具栏
- `BiographyBook`
  - 裁决：不作为独立 V1 核心页面
  - 处理方式：先并入 `Preview` 的导出配置，V2 再独立

### 9.3 V1 延后隐藏

- `Analytics`
- `QuickActions`
- `Notifications`

原因：

- 这些页面展示的是“系统已成熟后的运营和增强体验”
- 当前会分散 agent 注意力，导致先做漂亮壳子，后补核心数据链

## 10. 技术宪章

### 10.1 前端

- 保留 React + Vite + Tailwind
- 不重写为 Next.js
- 不引入为了解决不存在问题的复杂状态管理
- 所有服务端调用统一收口到 `src/api/` 或 `src/services/`
- 页面组件禁止直接写裸 `fetch`

### 10.2 后端

- 使用 Node.js + TypeScript 单体服务
- V1 优先选择 Express；理由是团队与 agent 协作的认知成本最低
- 不做微服务拆分
- 所有 AI 调用必须走后端
- 前端生产环境禁止持有模型 API Key
- 后端必须提供统一的模型接入层，不允许业务代码直接绑定单一供应商 SDK

### 10.3 数据与存储

- 主数据库：PostgreSQL
- 文件存储：阿里云 OSS
- 任务处理：先保证异步任务模型成立

对异步任务的具体裁决：

- Stage A 可以先用“数据库任务表 + 后台 worker”或简化队列实现
- 到 Stage B 前，若任务量上来，再升级到 Redis + BullMQ
- 结论：异步任务是必须的，Redis 不是第一天必须的

### 10.4 AI 接入宪章

- 必须设计统一的 `AI Gateway` 或 `Model Provider Layer`
- 业务层只能调用平台内部抽象能力，不能直接耦合具体模型 SDK
- 至少要抽象下面 4 类能力：
  - 文本生成
  - 图像理解
  - 音频转写
  - 结构化提取
- 每次 AI 调用必须记录：
  - provider
  - model
  - task_type
  - request_id
  - token 或计费信息
  - 延迟
  - 成功/失败状态
- Prompt 模板必须与 provider 实现分离
- 必须支持按任务类型选择默认模型，而不是全系统只绑定一个模型
- 必须允许未来增加新 provider，而不改动上层业务流程

对多模型能力的正式裁决：

- 正确目标不是“一开始同时接完所有模型”
- 正确目标是“第一天就留出稳定的多模型接入口”
- V1 可以只先打通少量 provider
- 但架构上不得把系统写成 Gemini 专用、Claude 专用或 OpenAI 专用

### 10.5 部署

- 先按阿里云 Web 部署设计
- 前后端边界必须清晰
- API 设计必须可复用到移动 Web、小程序、App

### 10.6 安全与隐私硬约束

- 不在浏览器 localStorage 保存生产用模型密钥
- 不把真实用户素材直接暴露给第三方前端 SDK
- 要有基本的数据删除与导出能力预留
- 导出链接、素材访问链接默认短时有效
- 不同 provider 的密钥必须在服务端分环境管理，不能散落在业务代码中

## 11. V0 与 V1 的交付边界

### 11.1 V0 必须完成

- 建立共享领域模型与术语表
- 建立 API 契约草案
- 移除 UI 中的 mock 依赖路径
- 梳理页面去留
- 去掉本地 API Key 驱动的产品逻辑
- 让前端具备“可接真实后端”的骨架

### 11.2 V1 必须完成

- 真实项目流
- 真实素材流
- 真实分析流
- 真实事件流
- 真实章节流
- 基础导出流

### 11.3 V1 明确不做

- 视频
- 家庭协作
- 小程序
- 精美出版物
- 开放公众注册增长玩法

## 12. 验收标准

### 12.1 Stage A 验收标准

- 你能独立完成注册/登录
- 你能创建 1 个项目并填写主人公信息
- 你能上传至少 3 份真实素材
- 你能看到至少 3 个事件被生成并编辑
- 你能得到至少 1 个章节草稿
- 你能修改章节并保存
- 你能导出一份可阅读初稿
- 全过程不需要开发者改数据库或手工补数据

### 12.2 文档验收标准

后续任何 agent 拿到本文件后，应能明确回答：

- 产品是什么
- V1 到底做什么
- 哪些页面保留
- 哪些页面延后
- 哪些实体必须存在
- 什么是主链路
- 哪些事情现在绝对不能做

## 13. 对下游 agent 的硬性约束

1. 没有先读本文件，不得开始大规模编码。
2. 不得擅自把项目改成聊天产品、相册产品或纯内容管理系统。
3. 不得绕过 `Event` 层，直接从素材生成无法回溯的最终长文。
4. 不得在 V1 里引入视频、家庭协作、公众广场等超范围能力。
5. 不得重写前端框架。
6. 不得把模型密钥继续放在前端本地存储里当正式方案。
7. 任何会影响领域模型的改动，都必须先更新本文件或提出变更申请。

## 14. Owner 裁决记录

以下事项已完成裁决，可直接作为后续 agent prompt 与排期依据。

### 决策 1：V1 登录方式

#### 方案 A：仅 owner 私有登录

- 优点：
  - 最快
  - 认证成本最低
  - 适合先验证个人闭环
- 缺点：
  - 很快会在家庭阶段返工
  - 无法提前验证真实注册流程

#### 方案 B：邮箱登录或 magic link

- 优点：
  - 足够支撑自己用、家人用、未来公众用
  - 比短信登录轻
  - 更适合早期 Web MVP
- 缺点：
  - 比 owner 私有登录多一层实现成本
  - 需要邮件通道

#### 方案 C：手机号验证码登录

- 优点：
  - 更符合中文用户习惯
  - 面向家人和公众转化更顺
- 缺点：
  - 短信供应商、成本、风控复杂度显著增加
  - 对当前 Stage A 明显过重

#### Owner 已裁决

- 已选择 `方案 B`

### 决策 2：家庭阶段的协作模型

#### 方案 A：一个项目，多人受邀协作

- 优点：
  - 最符合“家庭一起补完一份人生回忆录”
  - 不会把同一人生故事拆散
- 缺点：
  - 需要角色与权限设计

#### 方案 B：每人一个项目，后续再合并

- 优点：
  - 简单
  - 权限隔离直观
- 缺点：
  - 会把家庭阶段的价值削弱成“各写各的”
  - 后续合并代价高

#### Owner 已裁决

- 已选择 `方案 A`

### 决策 3：多模型接入策略的最小落地方式

#### 已确认的大方向

- 必须支持多模型接入
- 不锁死单一供应商
- 允许未来接入 Gemini、Claude、OpenAI、DeepSeek、Kimi、MiniMax 等

#### 仍需补充的子决策

##### 方案 A：先做统一接口，只接 2 个 provider 作为 V1 起点

- 参考组合：
  - OpenAI + Gemini
  - OpenAI + Claude
  - Gemini + Claude
- 优点：
  - 架构正确且实现成本可控
  - 足够验证 provider 抽象是否成立
  - 不会把 V1 排期拖垮
- 缺点：
  - 其余模型要在 V1 后补接

##### 方案 B：V1 直接接入 4 到 6 个 provider

- 优点：
  - 供应商覆盖更全
  - 更早验证国产与海外模型差异
- 缺点：
  - 接口、认证、限额、错误处理复杂度急剧上升
  - 极易拖慢主链路闭环

##### 方案 C：先定义统一接口，V1 只接 1 个默认 provider，但保留 adapter 框架

- 优点：
  - 实现最快
  - 最利于尽快打通 owner 闭环
- 缺点：
  - 虽然架构留口，但多 provider 真实性验证不足
  - V1 后仍有较高补接不确定性

#### Owner 已裁决

- 已选择 `方案 A`
- 执行解释：
  - 先做统一接口
  - V1 只接 2 个 provider
  - 目标是验证多模型抽象成立，而不是在 V1 一次性接全
- 实施建议：
  - 文本与结构化生成默认 `OpenAI` 或 `Claude`
  - 图像理解可优先 `Gemini`
  - 音频转写优先阿里云 ASR 或专用 ASR 服务

## 15. 下一份文档的责任边界

本文件之后，下一批应生成的文档应是：

1. `02_DOMAIN_MODEL.md`
2. `03_INFORMATION_ARCHITECTURE.md`
3. `04_API_CONTRACT.md`
4. `05_V0_EXECUTION_PLAN.md`
5. 面向前端 agent 与后端 agent 的执行 prompt

在这些文档生成之前，不应直接让多个 agent 大规模并行写代码。
